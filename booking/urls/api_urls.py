from django.conf.urls import url, include


urlpatterns = [
    # url(
    #     regex=r'^send-signature-reminders/$',
    #     view=SendSignatureRemindersApi.as_view(),
    #     name='send-signature-reminders'
    # ),
]